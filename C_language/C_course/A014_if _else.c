#include <stdio.h>
#include <stdlib.h>

int main() {

    /*
        if (condição) {
            comandos;
        } else {

        }
    
        Obs:
        Se tiver apenas um comando não precisa de chaves
        não precisa de ; apos as chaves
    */

   if (1) {
      printf("Verdadeiro!\n\n");
   } else {
      printf("Falso!\n\n");
   }

    return 0;
}